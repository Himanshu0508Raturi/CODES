#include<stdio.h>
//This programme is to calculate perimeter of rectangle.
float main()
{
    float a,b;
    printf("Enter the length(L) of rectangle\n",a);
    scanf("%f",&a);

    printf("Enter the breadth(B) of rectangle\n",b);
    scanf("%f",&b);

    printf("Perimeter of rectangle is(P):%f\n",2*(a+b));
    printf("thank you");
    return 0;
    







}
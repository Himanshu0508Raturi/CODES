#include <stdio.h>
// This programme is to calculate the cube of a number(n).
float main()
{
    float r;
    printf("enter the radius\n", r);
    scanf("%f", &r);

    printf("Its cube is:%f", 3.14 * r * r);

    return 0;
}
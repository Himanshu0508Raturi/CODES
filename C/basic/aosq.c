#include <stdio.h>
// to calculate area of square(given side).
float main()
{
    float a;
    printf("enter the side of square \n", a);
    scanf("%f", &a);
    printf("The area of square is: %f", a * a);
    return 0;
}